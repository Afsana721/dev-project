Javascript Object, Properties & methods
//create javascript Object
    - const person = {
        name: "xyz",
        age: 25
    };

Example: let person = {
    firstname: "abc",
    lastname: 'def',
    age: 25,
    weight: 60
}

//access the object properties.
console.log(person.firstname);
console.log(person['age']);

//create new data object using Object constructor.
const data = new Object();
console.log(data);

// add new property to the existing object person

person["height"] = 5.5;
person.city = "Austin";
console.log(person);

//function inside the object - called method and method is also a kind of property 
of the object. This function can be named or not use name.
//create an object for employee
let employee = {
    empid:111,
    lastname: "Scott",
    fistname: "Lee",
    age: 35,
    city: "Austin",
    job: "teacher",
    salary: 5000,
    bonus: function () {
        return ((this.salary * 10)/100)
    }
};

//access employee object
//console.log(employee);
console.log(employee.bonus());